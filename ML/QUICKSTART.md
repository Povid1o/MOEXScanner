# 🚀 Быстрый старт

## Проблема: "ModuleNotFoundError: No module named 'seaborn'"

### Решение за 30 секунд:

1. **В Cursor:** Нажмите `Ctrl+Shift+P` → `Python: Select Interpreter` → Выберите `.\ML\venv\Scripts\python.exe`

2. **Или в терминале:** Всегда используйте полный путь:
   ```powershell
   .\ML\venv\Scripts\python.exe your_script.py
   ```

---

## Два способа работы

### 1️⃣ Jupyter Notebooks (для экспериментов)

```powershell
cd ML
.\start_jupyter.ps1
```

✅ Все библиотеки доступны автоматически

### 2️⃣ Python скрипты в Cursor (для разработки)

**Важно:** Сначала выберите интерпретатор (см. выше)

```python
import seaborn as sns  # Теперь работает!
import pandas as pd
```

---

## Проверка настройки

```powershell
cd ML
.\venv\Scripts\python.exe test_setup.py
```

Если видите `[OK]` для всех библиотек - всё настроено правильно! ✅

---

## Установка новых библиотек

```powershell
cd ML
.\venv\Scripts\python.exe -m pip install название_библиотеки
```

**❌ НЕ используйте просто `pip install`** - установится в системный Python!

---

📚 **Подробное руководство:** [GUIDE.md](GUIDE.md)

