# 📚 Полное руководство по работе с ML проектом

## 🎯 Основная проблема и её решение

### Проблема: "ModuleNotFoundError: No module named 'seaborn'"

**Причина:** Cursor (и VS Code) по умолчанию использует системный Python, а не Python из виртуального окружения `venv`.

**Решение:** Нужно настроить Cursor для использования правильного интерпретатора.

---

## 🔧 Настройка Cursor для работы с проектом

### Шаг 1: Выбор интерпретатора Python

1. Откройте любой `.py` файл в папке `ML/`
2. Нажмите `Ctrl+Shift+P` (или `Cmd+Shift+P` на Mac)
3. Введите: `Python: Select Interpreter`
4. Выберите: `.\ML\venv\Scripts\python.exe`

**Или вручную:**
- Внизу экрана Cursor найдите индикатор Python (например, `Python 3.12.0`)
- Кликните на него
- Выберите `.\ML\venv\Scripts\python.exe`

### Шаг 2: Проверка настройки

Создайте тестовый файл `ML/test_imports.py`:

```python
import seaborn as sns
import pandas as pd
import numpy as np
import matplotlib.pyplot as plt

print("✅ Все библиотеки успешно импортированы!")
print(f"Seaborn версия: {sns.__version__}")
print(f"Pandas версия: {pd.__version__}")
```

Запустите его через `F5` или правой кнопкой → "Run Python File in Terminal"

Если видите версии библиотек - всё настроено правильно! ✅

---

## 📁 Структура проекта

```
MOEXScanner/
├── ML/                          # ML часть проекта
│   ├── venv/                    # Виртуальное окружение (ВСЕ библиотеки здесь!)
│   ├── data/                    # Данные
│   │   └── MOEX_DATA/          # Данные по акциям MOEX
│   ├── features/                # Модули для работы с данными
│   │   └── Loaders/
│   │       ├── load_prices.py  # Загрузка цен
│   │       └── price_cleaner.py # Очистка данных
│   ├── models/                 # Обученные модели
│   ├── backtest/               # Бэктестинг
│   ├── utils/                  # Утилиты
│   ├── config.py              # Конфигурация проекта
│   ├── requirements.txt        # Список зависимостей
│   └── *.ipynb                 # Jupyter notebooks
├── backend/                    # Go backend
└── front/                      # Frontend
```

---

## 🐍 Два способа работы с Python кодом

### Способ 1: Jupyter Notebooks (для экспериментов)

**Когда использовать:**
- Исследовательский анализ данных
- Быстрое тестирование идей
- Визуализация данных
- Интерактивная работа

**Как запустить:**
```powershell
cd ML
.\start_jupyter.ps1
```

**Преимущества:**
- ✅ Все библиотеки доступны автоматически (venv активирован)
- ✅ Интерактивная работа с данными
- ✅ Визуализация прямо в браузере

### Способ 2: Python скрипты в Cursor (для разработки)

**Когда использовать:**
- Разработка модулей и функций
- Создание переиспользуемого кода
- Интеграция с остальным проектом

**Важно:** Перед запуском кода в Cursor:
1. Убедитесь, что выбран правильный интерпретатор (см. выше)
2. Или используйте полный путь к Python из venv

**Пример запуска:**
```powershell
# В терминале Cursor
cd ML
.\venv\Scripts\python.exe your_script.py
```

---

## 🔍 Как понять, какой Python используется?

### В Cursor:

1. Откройте терминал (`Ctrl+``)
2. Выполните:
   ```powershell
   python --version
   where python
   ```
3. Должно показать путь к `ML\venv\Scripts\python.exe`

### В Jupyter:

Jupyter автоматически использует Python из venv, так как запускается через скрипт `start_jupyter.ps1`

---

## 📦 Установка новых библиотек

### Правильный способ:

```powershell
cd ML
.\venv\Scripts\python.exe -m pip install название_библиотеки
```

### Или через requirements.txt:

```powershell
cd ML
.\venv\Scripts\python.exe -m pip install -r requirements.txt
```

**❌ НЕ используйте просто `pip install`** - это установит в системный Python!

---

## 🚀 Типичные сценарии работы

### Сценарий 1: Быстрый эксперимент с данными

1. Запустите Jupyter:
   ```powershell
   cd ML
   .\start_jupyter.ps1
   ```
2. Создайте новый notebook
3. Импортируйте данные:
   ```python
   from features.Loaders.load_prices import load_prices
   import pandas as pd
   
   # Загрузка данных
   df = load_prices('SBER', '1D')
   ```

### Сценарий 2: Разработка нового модуля

1. Откройте Cursor
2. Убедитесь, что выбран интерпретатор `ML\venv\Scripts\python.exe`
3. Создайте файл в `ML/features/` или `ML/utils/`
4. Напишите код и запустите через `F5`

### Сценарий 3: Запуск существующего скрипта

```powershell
cd ML
.\venv\Scripts\python.exe example_usage.py
```

---

## ⚠️ Частые ошибки и их решение

### Ошибка 1: "ModuleNotFoundError: No module named 'seaborn'"

**Причина:** Cursor использует системный Python

**Решение:**
1. Выберите правильный интерпретатор (см. выше)
2. Или используйте полный путь:
   ```powershell
   .\ML\venv\Scripts\python.exe your_script.py
   ```

### Ошибка 2: "ImportError: cannot import name 'X' from 'Y'"

**Причина:** Неправильный PYTHONPATH

**Решение:**
```python
import sys
from pathlib import Path

# Добавьте путь к ML в PYTHONPATH
ml_path = Path(__file__).parent.parent / 'ML'
sys.path.insert(0, str(ml_path))
```

### Ошибка 3: Библиотека установлена, но не находится

**Причина:** Установлена в другой Python

**Решение:**
1. Проверьте, где установлена:
   ```powershell
   .\ML\venv\Scripts\python.exe -m pip list | findstr seaborn
   ```
2. Если не найдена, установите в правильный venv:
   ```powershell
   .\ML\venv\Scripts\python.exe -m pip install seaborn
   ```

---

## 🎓 Лучшие практики

1. **Всегда работайте в venv** - не используйте системный Python
2. **Для экспериментов** - используйте Jupyter Notebooks
3. **Для разработки модулей** - используйте Python скрипты в Cursor
4. **Перед запуском кода** - проверяйте, какой интерпретатор выбран
5. **Устанавливайте библиотеки** только через venv Python

---

## 📞 Проверка работоспособности

Выполните этот тест, чтобы убедиться, что всё настроено правильно:

```python
# test_setup.py
import sys
from pathlib import Path

print("=" * 50)
print("ПРОВЕРКА НАСТРОЙКИ ПРОЕКТА")
print("=" * 50)

# 1. Проверка Python пути
python_exe = sys.executable
print(f"\n1. Python интерпретатор: {python_exe}")
if 'venv' in python_exe or 'ML' in python_exe:
    print("   ✅ Используется venv")
else:
    print("   ⚠️  Используется системный Python!")

# 2. Проверка библиотек
try:
    import pandas as pd
    import numpy as np
    import seaborn as sns
    import matplotlib.pyplot as plt
    import sklearn
    print("\n2. Библиотеки:")
    print(f"   ✅ pandas {pd.__version__}")
    print(f"   ✅ numpy {np.__version__}")
    print(f"   ✅ seaborn {sns.__version__}")
    print(f"   ✅ matplotlib {plt.matplotlib.__version__}")
    print(f"   ✅ scikit-learn {sklearn.__version__}")
except ImportError as e:
    print(f"\n2. ❌ Ошибка импорта: {e}")

# 3. Проверка путей проекта
try:
    from config import BASE_DIR, DATA_DIR
    print("\n3. Пути проекта:")
    print(f"   ✅ BASE_DIR: {BASE_DIR}")
    print(f"   ✅ DATA_DIR: {DATA_DIR}")
except ImportError as e:
    print(f"\n3. ❌ Ошибка импорта config: {e}")

print("\n" + "=" * 50)
print("Проверка завершена!")
print("=" * 50)
```

Сохраните как `ML/test_setup.py` и запустите:
```powershell
.\ML\venv\Scripts\python.exe ML\test_setup.py
```

---

## 🎯 Краткая памятка

| Действие | Команда |
|----------|---------|
| Запустить Jupyter | `cd ML` → `.\start_jupyter.ps1` |
| Запустить Python скрипт | `.\ML\venv\Scripts\python.exe script.py` |
| Установить библиотеку | `.\ML\venv\Scripts\python.exe -m pip install название` |
| Проверить интерпретатор | В Cursor: `Ctrl+Shift+P` → `Python: Select Interpreter` |
| Проверить установленные пакеты | `.\ML\venv\Scripts\python.exe -m pip list` |

---

**Главное правило:** Всегда используйте Python из `ML\venv\Scripts\python.exe`! 🐍

